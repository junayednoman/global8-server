export const DJ_EVENT_ALLOWED_SORT_FIELDS = new Set([
  "createdAt",
  "updatedAt",
  "title",
  "date",
]);

export const DJ_EVENT_ORDER_BY_VALUES = new Set(["asc", "desc"]);

export const DJ_EVENT_TIME_FILTER_VALUES = new Set(["recent", "past"]);
